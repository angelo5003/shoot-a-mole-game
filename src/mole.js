class Mole {
  constructor() {}
  //1. draw the hole and the moles
  drawMole() {
    this.createHole();
  }
  // 2.create a hole where the digletts are hiding
  createHole() {
    circle(50, 30, 50);
  }
}
